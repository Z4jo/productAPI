package com.api.product.repositary;

import com.api.product.entity.RestInfo;
import org.springframework.data.repository.CrudRepository;

public interface RestInfoRepository extends CrudRepository<RestInfo,Integer> {
}
