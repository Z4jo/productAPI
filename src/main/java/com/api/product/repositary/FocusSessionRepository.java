package com.api.product.repositary;

import com.api.product.entity.FocusSession;
import org.springframework.data.repository.CrudRepository;

public interface FocusSessionRepository extends CrudRepository <FocusSession,Integer>{
}
